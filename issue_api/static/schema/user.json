{
    "type": "object",
    "required": ["name", "api_key"],
    "properties": {
        "name": {
            "type": "string",
            "maxLength": 32,
            "description": "Name of the user"
        },
        "api_key": {
            "type": "string",
            "maxLength": 32,
            "description": "Api key for the user"
        }
    }
}